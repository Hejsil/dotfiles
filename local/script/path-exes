#!/bin/execlineb -P
importas path PATH
pipeline { echo "$path" }
pipeline { tr ":" "\n" }
pipeline { sort -u }
pipeline { redirfd -w 2 /dev/null xargs fd . -L --type f }
pipeline { xargs basename -a }
sort -u
