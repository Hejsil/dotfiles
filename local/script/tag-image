#!/bin/sh

image=$1
out=$2

fzf-img-mpv-window "$image" &
mpv_pid=$!
sleep 0.1

name=$(tag-namer "" "fzf-img-no-mpv" "")
if [ -z "$name" ]; then
    kill "$mpv_pid"
    exit 255
fi

tag-rename "$image" "$out" "$name"
kill "$mpv_pid"
