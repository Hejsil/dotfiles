#!/bin/sh

sink=$(pactl get-default-sink)
src=$(pactl get-default-source)
videos_dir=$(xdg-user-dir VIDEOS)
container='mp4'

exec gpu-screen-recorder -v no -w DP-1 -q ultra -ac opus -f 60 -r 600 -c "${container}" \
    -o "${videos_dir}" -a "${sink}.monitor" -a "${src}"
