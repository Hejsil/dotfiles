#!/bin/execlineb -s1
pipeline { echo }
fzf --disabled --delimiter ":" --no-extended --ansi \
    --bind "change:reload:rg $1 --color=always --hidden --line-number -- {q} || true" \
    --preview "fzf-rg-bat {2} {1}"
