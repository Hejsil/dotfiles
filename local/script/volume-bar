#!/bin/execlineb -P
pipeline { pactl subscribe }
pipeline { stdbuf -o L grep -F "'change' on sink " }
pipeline { forstdin _ pulsemixer --get-volume }
pipeline { stdbuf -o L cut -f1 "-d " }
pipeline { stdbuf -o L uniq }
wob
