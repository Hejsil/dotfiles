#!/bin/execlineb -P
pipeline { pactl subscribe }
pipeline { rg --line-buffered "'change' on sink " }
pipeline {
    forstdin _ pipeline { pulsemixer --get-volume } cut -f1 "-d "
}
pipeline { stdbuf -o L uniq }
xob
